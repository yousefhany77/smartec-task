import React, { useRef } from "react";

interface Props {
  canvasRef: React.RefObject<HTMLCanvasElement>;
}
function useImageEditor({ canvasRef }: Props) {
 

  return;
}
export default useImageEditor;
